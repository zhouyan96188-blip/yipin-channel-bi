# 逸品看板 · 更新机制

## 数据怎么进看板

```
data/yipin_data.json   ← 数据源（每月/每周更新这一个文件）
        ↓  scripts/inject.py
     index.html        ← 数据固化在 HTML 里，页面打开即是逸品的数据
```

数据**直接写在 index.html 里**，不依赖任何运行时补丁、不依赖外部接口。

## 自动更新

`.github/workflows/update-dashboard.yml`

- **每周三 09:00（北京时间）自动跑**（cron `0 1 * * 3`，UTC 01:00）
- 也可以随时手动跑：仓库 → Actions → 「更新逸品看板数据」→ Run workflow
- 跑的内容：读 `data/yipin_data.json` → 注入 `index.html` → 有变化就自动 commit + push → GitHub Pages 几分钟后生效

## 每周三要做的事

Actions 只负责「把 json 灌进 html」。**json 里的数字需要先更新**，两种方式：

1. **让 Claude 更新**（推荐）：说一句「更新看板数据」，Claude 会从 BI + 预算表 + 结算表重新采集，生成新的 `yipin_data.json` 传上来，Actions 自动接手。
2. **手动改**：直接编辑 `data/yipin_data.json`，提交后 Actions 会在下个周三跑；想立刻生效就去 Actions 手动 Run 一次。

## 数据来源对照

| 看板字段 | 来源 |
|---|---|
| 新增目标 / 预算 / 负责人 | 逸品渠道预算与目标 →「N月」sheet（F/G/S 列） |
| 实际新增 / 消耗 / 充值 | BI `query_product_summary`，15 个在投产品 |
| 逐日趋势 | BI 按天实查 |
| 订单流水 / 打款 / 退款 | 渠道结算明细-N月（请款日期非空的行） |
| 商务收款 | 逸品广告销售明细，按付款时间筛当月 |

## 口径

- **实际消耗 = BI 投放消耗**，不是打款。月初结算滞后，用打款会失真；打款单列在「预算与支出」页。
- 完成率/消耗率的红黄绿以**时间进度**为基准（当月已过天数 ÷ 当月总天数）。
